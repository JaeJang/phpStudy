/*function swipe() {
	$(".back").hide();
	$(".front").on("swipeleft", function(event) {
		$(this).hide();
		var parent = $(this).parent();
		parent.find(".back").show();
	});
	
	$(".back").on("swiperight", function(event) {
		$(this).hide();
		var parent = $(this).parent();
		parent.find(".front").show();
	});
	
}*/

function flip(id) {
	/*var current = document.getElementById(id);
	var number = id.charAt((id.length - 1));
	if (current.className == "front") {
		var newID = "description" + number;
		var newElement = document.getElementById(newID);
		current.style.display = "none";
		newElement.style.display = "block";
	} else if (current.className == "back") {
		var newID = "recipeFront" + number;
		var newElement = document.getElementById(newID);
		current.style.display = "none";
		newElement.style.display = "block";
	}*/
}